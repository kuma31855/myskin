# API サーバー用（本番EC2では必ず設定）
PORT=3000

# MySQL
DB_HOST=localhost
DB_USER=myskin
DB_PASSWORD=myskin
DB_NAME=myskin
DB_PORT=3306

# CORS: フロントのURLをカンマ区切り（本番ドメインを追加）
# 例: https://your-domain.com,https://www.your-domain.com
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174
